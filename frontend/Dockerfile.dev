# 프론트엔드 개발용 Dockerfile (Vite HMR 지원)
FROM node:20-alpine

WORKDIR /app

# 패키지 설치를 위해 복사
COPY package.json package-lock.json ./
RUN npm install

# 소스 코드는 docker-compose의 volumes에서 매핑하므로 
# 여기서는 도커 빌드 시점에만 복사 (초기 의존성 확인용)
COPY . .

# Vite 기본 포트
EXPOSE 5173

# Vite 개발 서버 실행 (외부 접속 허용을 위해 --host 설정)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
